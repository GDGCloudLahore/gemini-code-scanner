name: Code Scan

on:
  pull_request:
    types: [opened, synchronize]

jobs:
  scan:
    runs-on: ubuntu-latest
    permissions:
      contents: read
      issues: write

    steps:
      # Step 1: Checkout the code
      - uses: actions/checkout@v3

      # Step 2: Debug Environment
      - name: Debug Environment
        run: env | grep INPUT_

      # Step 3: Debug Info
      - name: Debug Info
        run: |
          echo "Repository: ${{ github.repository }}"
          echo "Branch Name: ${{ github.head_ref || github.ref_name }}"

      # Step 4: Run the custom action
      - name: Gemini Code Scanner
        uses: ./
        with:
          gemini_api_key: ${{ secrets.GEMINI_API_KEY }}
          github_token: ${{ secrets.MY_GITHUB_TOKEN }}  # Pass the token as MY_GITHUB_TOKEN
          repo_name: ${{ github.repository }}  # Dynamically fetch the repo name
          branch_name: ${{ github.head_ref || github.ref_name }}  # Correct branch name logic
